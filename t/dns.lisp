(in-package :cl-user)
(defpackage dns-test
  (:use :cl
        :dns
        :prove))
(in-package :dns-test)

;; NOTE: To run this test file, execute `(asdf:test-system :dns)' in your Lisp.

(plan nil)

;; blah blah blah.

(finalize)
